{
  "private": true,
  "name": "agent-chat",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace agent-chat-api",
    "dev:ui": "npm run dev --workspace agent-chat-ui"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
